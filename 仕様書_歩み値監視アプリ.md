# 仕様書_歩み値監視アプリ（StockTools）

> 本仕様書は、添付されたソースコード（App.xaml / App.xaml.cs / MainWindow.xaml /
> MainWindow.xaml.cs / RegionSelector.xaml(.cs) / DetectedImageWindow.xaml(.cs) /
> ImageUtils.cs / AssemblyInfo.cs / StockTools.csproj / StockTools.sln /
> 歩み値監視_メモ.txt）を正として、シニアソフトウェアアーキテクトの視点で
> リバースエンジニアリングし作成した「唯一の公式仕様」です。
> ソースコードに明記のない意図・設計判断については **[推測]** と明記しています。

- 対象バージョン: 添付ソース一式（2026年時点のスナップショット）
- 対象OS: Windows（WPF / .NET 8.0-windows, `net8.0-windows`）
- プロジェクト名: `StockTools`（ソリューション: `StockTools.sln`）

---

## 1. アプリ概要

「歩み値監視ツール」は、証券会社等の取引ツール画面上に表示される歩み値（時刻・株数・約定単価の並び）を
**画面キャプチャ + OCR** によって定期的に読み取り、指定した出来高しきい値を超える大口取引を検出したら、
検出箇所をマークアップしたスクリーンショットを別ウィンドウに表示し、音声で通知するデスクトップアプリです。

付随機能として、WebView2 を用いた「スクリーニング」タブ（外部Webアプリの埋め込み表示）を持ちます。

### 主な技術要素
| 領域 | 使用技術 |
|---|---|
| UIフレームワーク | WPF（.NET 8.0-windows） |
| OCR | Tesseract（NuGet: `Tesseract` 5.2.0、`tessdata` は `jpn+eng` を使用） |
| 画面キャプチャ | `System.Drawing.Common`（`Graphics.CopyFromScreen`） |
| 音声合成 | `System.Speech`（`SpeechSynthesizer`） |
| 外部Web埋め込み | `Microsoft.Web.WebView2` |
| その他将来利用想定 | `ExcelDataReader`, `HtmlAgilityPack`, `LiveCharts.Wpf.NetCore3`, `SkiaSharp`, `YahooFinanceApi`
（※現時点のコード上では歩み値監視機能から未使用。スクリーニング／今後の機能タブ向けと推測 **[推測]**） |

---

## 2. アーキテクチャ（クラス構成・責務）

現状は MVVM ではなく **Window の code-behind に UI制御とビジネスロジックが同居する構成**です。
責務分離は下表の通りクラス（ファイル）単位で行われています。

```
StockTools/
├── App.xaml / App.xaml.cs            … アプリケーションエントリポイント
├── MainWindow.xaml / .xaml.cs        … メイン画面（View + 監視ロジック本体）
├── RegionSelector.xaml / .xaml.cs    … キャプチャ範囲選択用オーバーレイウィンドウ
├── DetectedImageWindow.xaml / .xaml.cs … 検出結果画像のポップアップ表示ウィンドウ
├── Helpers/
│   ├── ImageUtils.cs                 … Bitmap→BitmapSource 変換ユーティリティ
│   ├── AmountUnitHelper.cs           … 表示単位（億円/百万円）の除数・表示テキスト変換
│   └── AppSettingsProvider.cs        … settings.json の読込／保存（既定値の自動生成含む）
├── Models/
│   └── AppSettings.cs                … アプリ設定モデル（AmountUnit 列挙型を含む）
├── Views/
│   └── FutureFeaturesView.xaml / .xaml.cs … 「今後の機能」タブ用UserControl（拡張タブの雛形）
├── settings.json                     … 実行時に自動生成されるアプリ設定ファイル（実行ファイルと同階層）
├── tessdata/tessdata-main/           … Tesseract 学習済みデータ（eng / jpn / jpn_vert）
└── Resources/favicon.ico
```

### 2.1 App（App.xaml / App.xaml.cs）
- `Application` の起点。`StartupUri="MainWindow.xaml"` により起動時に `MainWindow` を表示。
- 現状カスタムロジックなし。

### 2.2 MainWindow（View兼コントローラ）
- タブ構成: 「歩み値監視」「スクリーニング」「今後の機能」（`Views.FutureFeaturesView` を表示する拡張用タブ）。
- 責務:
  - 起動時のアプリ設定読込（`AppSettingsProvider.Load()`）
  - 画面項目（しきい値・遅延時間・画像表示秒数・マークアップ余白・音声通知文言）の入力とバリデーション
  - キャプチャ領域選択（`RegionSelector` 呼び出し）
  - 監視ループ（`MonitorLoop`）の起動・停止
  - 画面キャプチャ → 前処理（`PreprocessImage` / `Dilate`）→ OCR（Tesseract）→ 結果パース →
    しきい値判定 → マークアップ描画（表示単位は `AmountUnitHelper` 経由で取得） → 検出時通知（画像ウィンドウ表示 + 音声）
  - タブ切替時のウィンドウ幅アニメーション（`AnimateWindowWidth`）
- 主要フィールド:
  - `captureRegion` : 監視対象のスクリーン座標範囲
  - `isMonitoring` : 監視中フラグ
  - `appSettings` : `AppSettingsProvider.Load()` で読み込んだ `Models.AppSettings`（表示単位など）
  - `lastValidXxx` 系フィールド群 : 入力バリデーションNG時に直前の正常値へロールバックするための退避領域
- 主要メソッド:
  - `Window_Loaded` : アプリ設定読込、各入力欄の初期値設定、WebView2初期化
  - `StartMonitoring_Click` / `StopMonitoring_Click` : 監視の開始／終了
  - `MonitorLoop` : 監視のメインループ（1秒間隔でポーリング）
  - `PreprocessImage` : HSV簡易変換による赤字／緑字の黒強調＋二値化
  - `Dilate` : 4近傍膨張処理（OCR精度向上のための文字太らせ）
  - `TryUpdateParameter<T>` : 汎用の入力バリデーション＆ロールバック共通処理

### 2.3 RegionSelector
- 半透明の全画面オーバーレイ上でマウスドラッグにより監視対象の矩形領域を選択させるウィンドウ。
- `TaskCompletionSource<System.Drawing.Rectangle>` を介して選択結果を呼び出し元（MainWindow）へ非同期で返す。
- DPIスケーリングを考慮した座標補正を実施。

### 2.4 DetectedImageWindow
- 検出時にマークアップ済みスクリーンショットを表示するだけの単純なポップアップウィンドウ。
- `MainWindow` 側で `DispatcherTimer` により一定秒数後に自動クローズされる。

### 2.5 ImageUtils（StockTools.Helpers 名前空間）
- `System.Drawing.Bitmap` → WPFの `BitmapSource` への変換ユーティリティ（縮小リサイズ込み、デフォルト0.8倍）。
- `DeleteObject`（GDIハンドル解放）をP/Invokeで呼び出し、`GetHbitmap()` のリークを防止。

### 2.6 AppSettings / AmountUnit（StockTools.Models 名前空間）
- `AppSettings` : アプリ設定全体を表すモデル。現時点では `AmountUnit`（表示単位）プロパティのみを持つ。
  今後設定項目を追加する場合は、このクラスにプロパティを追加するだけでよい設計。
- `AmountUnit`（enum） : `Million`（百万円） / `HundredMillion`（億円、既定値）。

### 2.7 AmountUnitHelper（StockTools.Helpers 名前空間）
- `AmountUnit` を「除数」「表示サフィックステキスト」に変換する静的ヘルパー。
- `GetDivisor(AmountUnit)` : 円単位の金額を指定単位に変換するための除数を返す。
- `GetSuffixText(AmountUnit)` : マークアップ描画に使う単位テキスト（例: `" 億円"`）を返す。
- 表示単位の種類を増やす場合は、このクラス内の `switch` 式に分岐を追加するのみでよい。

### 2.8 AppSettingsProvider（StockTools.Helpers 名前空間）
- `settings.json`（実行ファイルと同階層）の読込・保存を担当する静的クラス。
- `Load()` : ファイルが存在しない場合は既定値（`AmountUnit = HundredMillion`）で新規作成して返す。
  読込に失敗した場合（ファイル破損等）も既定値にフォールバックする。
- `Save(AppSettings)` : 設定をJSON形式で保存する。
- シリアライズには .NET ランタイム同梱の `System.Text.Json` を使用しており、追加のNuGetパッケージは不要。

### 2.9 FutureFeaturesView（StockTools.Views 名前空間）
- 「今後の機能」タブの中身を担う `UserControl`。現状はプレースホルダのテキスト表示のみ。
- **拡張パターンの雛形**として、今後新しい機能タブを追加する際は、
  `Views` 配下に同様の `UserControl`（命名規則: `〜View`）を新設し、
  `MainWindow.xaml` の対応する `TabItem` からその `UserControl` を参照する形に統一する
  （詳細は4章の命名規約、9章の変更履歴を参照）。

---

## 3. データフロー

```
[アプリ起動 / Window_Loaded]
   → AppSettingsProvider.Load() で settings.json を読込
     （無ければ既定値 AmountUnit=HundredMillion で自動生成）

[監視開始ボタン押下]
   → 入力バリデーション（しきい値/遅延/表示秒数/余白/音声文言）
   → RegionSelector でキャプチャ範囲を選択（非同期）
   → isMonitoring = true, MonitorLoop を別タスクで起動
        │
        ▼（1秒間隔ループ）
   ┌─────────────────────────────────────────┐
   │ 1. UIスレッドで最新の入力値を再バリデーション・取得   │
   │ 2. 指定領域をスクリーンキャプチャ（Bitmap）        │
   │ 3. PreprocessImage（色強調＋二値化）              │
   │ 4. Dilate（膨張処理）                            │
   │ 5. Tesseract OCR 実行（jpn+eng）                 │
   │ 6. OCR結果を単語単位でパースし                     │
   │    時刻→株数→単価 の並びを抽出                    │
   │ 7. 同一時刻でグループ化し出来高を合算               │
   │ 8. 合算出来高がしきい値以上ならマークアップ描画       │
   │    （矩形＋出来高＋約定代金[単位はappSettings.AmountUnit│
   │    に従い AmountUnitHelper で換算]をテキスト描画）    │
   │ 9. 検出があれば DetectedImageWindow 表示＋音声通知   │
   │10. 通知後は alertDelay 分待機、その後1秒待機して次周へ │
   └─────────────────────────────────────────┘
        │
        ▼
[監視終了ボタン押下] → isMonitoring = false → ループ終了
```

---

## 4. 命名規約（Conventions）

添付コードから読み取れる実運用ルールは以下の通りです。今後の改修・追加もこの規約に合わせること。

| 対象 | 規約 | 例 |
|---|---|---|
| クラス名 | PascalCase | `MainWindow`, `RegionSelector`, `ImageUtils` |
| メソッド名 | PascalCase（動詞始まり） | `StartMonitoring_Click`, `PreprocessImage`, `TryUpdateParameter` |
| private フィールド | camelCase | `isMonitoring`, `captureRegion`, `lastValidThreshold` |
| XAML の `x:Name`（入力欄） | `〜Box`（TextBox） | `ThresholdBox`, `DelayBox`, `MarginBox` |
| XAML の `x:Name`（ボタン） | `〜Button` | `StartMonitoringButton`, `StopMonitoringButton` |
| XAML の `x:Name`（チェックボックス） | `〜CheckBox` | （※本改修で `GroupByTimeCheckBox` は削除） |
| イベントハンドラ | `対象名_イベント名` | `StartMonitoring_Click`, `Window_Loaded`, `MainTabs_SelectionChanged` |
| バリデーション退避用フィールド | `lastValid〜` | `lastValidThreshold`, `lastValidSpeechText` |
| using エイリアス | 略語を用いる | `SD = System.Drawing`, `SWR = System.Windows.Shapes` |
| コメント | 日本語、処理の意図・注意点を簡潔に記述 | `// DPI補正`, `// 単位（億円/百万円）は baseline よりやや下げる...` |
| 名前空間 | `StockTools`（ルート）、`StockTools.Helpers`（ユーティリティ）、`StockTools.Models`（設定・データモデル）、`StockTools.Views`（タブ用UserControl） | |
| タブ用 `UserControl`（Views配下） | `〜View` | `FutureFeaturesView` |
| 設定モデルクラス（Models配下） | `〜Settings` | `AppSettings` |
| 変換・換算系ヘルパー | `〜Helper` | `AmountUnitHelper` |
| 設定ファイルの読込／保存を担う静的クラス | `〜Provider` | `AppSettingsProvider` |
| アプリ設定ファイル名 | `settings.json`（実行ファイルと同階層、固定名） | |

---

## 5. 主要仕様（詳細）

### 5.1 入力パラメータ（歩み値監視タブ）
| 項目 | UI要素 | 型/検証 | 初期値 |
|---|---|---|---|
| 株数しきい値 | `ThresholdBox` | 整数 | （未設定・空） |
| 音声通知の文字列 | `SpeechTextBox` | 空白不可 | `大口取引を検出しました` |
| 検出箇所マークアップの幅の余白 | `MarginBox` | 0以上の整数 | `10` |
| 通知後の遅延時間（ミリ秒） | `DelayBox` | 0以上の整数 | `1000` |
| 画像表示時間（秒） | `ImageDisplayDurationBox` | 0以上の整数 | `10` |

バリデーションNG時は `MessageBox` でエラー表示し、直前の正常値（`lastValid〜`）へ入力欄を戻す
（`TryUpdateParameter<T>` に共通化）。

> **本改修による変更点**: 「時刻でグループ化して合算」チェックボックス（`GroupByTimeCheckBox`）は
> 常にON相当の動作のみを行う仕様に変更し、UI・ロジックの両方から削除済み（詳細は変更履歴を参照）。

### 5.2 OCR設定
- エンジン: `TesseractEngine("./tessdata/tessdata-main", "jpn+eng", EngineMode.Default)`
- ホワイトリスト文字（時刻・数値検出用）: `0123456789:,.`
- ページセグメンテーションモード: `PageSegMode.SingleBlock`
- 時刻判定用正規表現: `^\d{2}:\d{2}:\d{2}$`

### 5.3 検出ロジック（時刻グループ化・合算方式）
1. OCR結果を単語単位で走査し、「時刻」→「株数」→「単価」の並びをステートマシン的に抽出。
2. 同一時刻キーごとに `(株数, 単価)` のリストを蓄積。
3. 時刻ごとの合計株数がしきい値以上であれば、その時刻に属する全ボックスの外接矩形を求めマークアップ。
4. 合計代金 = `Σ(株数 × 単価)`。表示単位は `appSettings.AmountUnit`（既定値: **億円**）に従い
   `AmountUnitHelper.GetDivisor()` で除算する（設定で百万円へ切替可能。詳細は5.6を参照）。
5. 表示テキスト: `{合計株数:N0}（{合計代金:N2}{AmountUnitHelper.GetSuffixText()}）` を
   マークアップ矩形の左側に描画。

### 5.4 検出時の通知
- `DetectedImageWindow` にマークアップ済み画像を表示（`ImageUtils.BitmapToBitmapSource` で変換）。
- `imageDisplaySeconds` 経過後に `DispatcherTimer` で自動クローズ。
- `SpeechSynthesizer.SpeakAsync` で音声通知文言を読み上げ。
- 通知後は `alertDelay` ミリ秒待機してから次の監視周期へ（連続通知の抑制）。

### 5.5 スクリーニングタブ
- `WebView2` で外部URL（GitHub Pages上の静的フロントエンド）を表示するのみ。
- タブ選択時、ウィンドウ幅を `420 → 1500` にアニメーションさせる（`AnimateWindowWidth`）。

### 5.6 アプリ設定（settings.json）
- 実行ファイルと同階層に配置される `settings.json` により、検出時の売買代金の表示単位を
  切替可能（`AmountUnit`: `HundredMillion`＝億円[既定] / `Million`＝百万円）。
- ファイルが存在しない場合、`AppSettingsProvider.Load()` が既定値で自動生成する。
- ファイル例:
  ```json
  {
    "AmountUnit": "HundredMillion"
  }
  ```
- 設定項目を追加する場合は `Models.AppSettings` にプロパティを追加するだけでよく、
  読込・保存処理（`AppSettingsProvider`）側の変更は不要（JSON自動シリアライズのため）。
- 表示単位の種類そのものを増やす場合（例: 兆円）は、`Models.AmountUnit` に列挙値を追加し、
  `Helpers.AmountUnitHelper` の `switch` 式に対応する除数・表示テキストを追加する。

### 5.7 「今後の機能」タブの拡張方式
- 「今後の機能」タブは `Views.FutureFeaturesView`（`UserControl`）を表示する構成に統一されている。
- 新しい機能タブを追加する際の標準手順:
  1. `Views/` 配下に `〜View.xaml` / `〜View.xaml.cs` を新設する（命名規則は4章参照）。
  2. `MainWindow.xaml` に新しい `TabItem` を追加し、その `Content` として作成した
     `UserControl` を配置する（`xmlns:views="clr-namespace:StockTools.Views"` を利用）。
  3. タブ固有のロジックは、原則として当該 `UserControl` のコードビハインド、または
     専用のヘルパー/サービスクラスに実装し、`MainWindow.xaml.cs` を肥大化させない。
- この方式により、既存タブ（歩み値監視・スクリーニング）のコードに影響を与えずに
  新機能タブを追加できる。

---

## 6. 依存パッケージ一覧（StockTools.csproj）

| パッケージ | バージョン | 用途 |
|---|---|---|
| ExcelDataReader / ExcelDataReader.DataSet | 3.7.0 | Excel読込（現状歩み値監視機能では未使用 **[推測]**） |
| HtmlAgilityPack | 1.12.1 | HTMLパース（スクリーニング機能向けと推測 **[推測]**） |
| LiveCharts.Wpf.NetCore3 | 0.9.8 | チャート表示（今後の機能タブ向けと推測 **[推測]**） |
| Microsoft.Web.WebView2 | 1.0.4078.44 | スクリーニングタブのWeb埋め込み |
| SkiaSharp / SkiaSharp.Views.WPF | 3.119.0 | 描画処理（現状未使用 **[推測]**） |
| System.Drawing.Common | 9.0.6 | 画面キャプチャ・画像処理全般 |
| System.Speech | 9.0.6 | 音声合成 |
| Tesseract | 5.2.0 | OCR |
| YahooFinanceApi | 2.3.3 | 株価データ取得（現状未使用 **[推測]**） |

`tessdata/tessdata-main/` 配下の `eng.traineddata` / `jpn.traineddata` / `jpn_vert.traineddata` は
ビルド出力へ常にコピー（`CopyToOutputDirectory=Always`）される。

---

## 7. 初期セットアップ手順（開発時メモより転記）

> 以下は初期開発時の作業メモ（`歩み値監視_メモ.txt`）をそのまま転記したものです。
> 新規に開発環境を構築する際の手順として引き続き有効です。

```
必要なインストール：
Tesseract OCR（Windows版）
	→ https://github.com/UB-Mannheim/tesseract/wiki（日本語対応可）
	※OCR（Optical Character Recognition / 光学文字認識）は、スキャナ画像や写真などに写っている「文字」をコンピュータが認識してテキストにする技術。

その他に必要な準備
	1. tessdata フォルダを準備
	以下のサイトから eng.traineddata をダウンロード：
	https://github.com/tesseract-ocr/tessdata

	ダウンロードしたファイルを Visual Studio プロジェクトのルートに「tessdata」フォルダを作成し、その中に配置します。

	🔸2. Visual Studio でファイルのビルドアクションを確認
	eng.traineddata のプロパティで
	ビルドアクション：コンテンツ
	出力ディレクトリにコピー：常にコピー
	に設定
	※ほかの言語（日本語など）を利用するなら、その言語も同様の対応が必要。

	🔸3. TesseractEngine の初期化コードが一致しているか確認
	csharp
	コピーする
	編集する
	using var ocr = new TesseractEngine(@"./tessdata", "eng", EngineMode.Default);
	"./tessdata" は実行ファイルからの相対パスです。パスが通っていなければ Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "tessdata") に置き換える方法もあります。

NuGet パッケージ（Visual Studioで追加）
	・Tesseract
	・System.Speech（音声合成）
	・System.Drawing.Common
```

> 補足: 現行コードでは `tessdata` のパスが `./tessdata/tessdata-main`、言語指定が `jpn+eng` に
> 変更されており、メモ作成時点（初期開発時）から構成が更新されています。この乖離はメモを
> 「当時の記録」として保持する目的のため、意図的に修正せず原文のまま記載しています。

---

## 8. 既知の制約・技術的負債（推測を含む）

- **[推測]** OCR解析・画像処理・通知処理は依然として `MainWindow.xaml.cs` に集中しており、
  責務がコードレベルで分離されていない（MVVMではない）。単体テストが書きにくい構造。
  （アプリ設定の読み書きは `AppSettingsProvider` に、タブ拡張は `Views` 配下の `UserControl` に
  分離済みだが、監視ループ本体のOCR解析・パーサー処理は未分離のまま。設計改善案は最後に記載）
- **[推測]** `PreprocessImage` / `Dilate` はピクセル単位で `GetPixel` / `SetPixel` を使用しており、
  領域サイズに比例して処理コストが大きい（後述のパフォーマンス改善案を参照）。
- OCRによる文字認識のため、取引ツール側のフォント・配色・レイアウトの変化に弱い（環境依存）。
- 通知音声・画像表示は簡易的なポーリング実装であり、高頻度な約定が続く相場急変時に
  検出漏れ・通知遅延が発生し得る **[推測]**。

---

## 9. 変更履歴

| 日付 | 内容 |
|---|---|
| （本改修 第1版） | 「時刻でグループ化して合算」チェックボックスをUI・ロジック双方から削除し、常時グループ化合算方式に一本化。検出時の代金表示単位を「百万円」から「億円」に変更。 |
| （本改修 第2版） | 表示単位を `settings.json`（`Models.AppSettings` / `Helpers.AmountUnitHelper` / `Helpers.AppSettingsProvider`）で切替可能な設計に変更。「今後の機能」タブを `Views.FutureFeaturesView`（UserControl）に切り出し、以降のタブ追加を既存タブに影響を与えず行える拡張パターンを確立。 |
