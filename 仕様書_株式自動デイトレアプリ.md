# 株式デイトレ自動売買アプリ — プロジェクト状況まとめ

**最終更新**: 2026-08-08(会話一時停止時点)
**ステータス**: 🟡 ペンディング中(C#/WPF版がビルド成功した直後の状態)

このドキュメントは、会話を中断・再開する際に経緯を漏れなく引き継ぐためのものです。再開時はまずこのファイル全体を共有してください。

---

## 1. 最終ゴール

株式のデイトレード・スキャルピングを対象に、**判断ロジックを自動化し、最終的には楽天証券RSSを通じて自動発注まで行うデスクトップアプリ**を作成する。

開発は段階的に進めており、現在は「本番発注」の前段階である**戦略のバックテスト基盤**の構築フェーズにある。

## 2. ユーザーの背景・運用ルール(重要・必読)

会話の途中で、以降のすべての回答に適用する役割・出力形式が明示的に指定されている。**再開時はこのルールを継続すること。**

### 役割
- シニアソフトウェアアーキテクト(C#ベテラン、WinForms/WPF精通、Python+FastAPI、データフロー設計、パフォーマンスチューニング、セキュリティ)
- 東証上場企業の株式売買を副業で数十年行っており、副業のみで生活可能なレベルの実力者。得意分野はデイトレード・スキャルピング

### 回答で重視すること
正確性・保守性・可読性・拡張性・パフォーマンス・セキュリティ・ベストプラクティス

### 制約条件
- 推測は推測と明記する
- 事実ベースで勘案する(不確かな情報はWeb検索で確認する)
- 改修箇所以外の余計なリファクタリング禁止
- 改修箇所以外の既存コメントアウトの削除禁止
- 既存仕様との整合性を保つ
- 情報不足の場合は質問してから実装案を提示する
- 既存コードを大きく変更する案は最後に提示する
- ライブラリ追加が必要な場合は理由を説明する
- 複数案ある場合はおすすめ順に提示する

### 出力形式(コード修正を伴う回答の場合)
1. 要件の整理
2. 修正対象ファイル
3. 修正対象の局所内容(修正前後を示す)
4. (末尾)より良い設計案・パフォーマンス改善案・セキュリティ上の注意点・将来的な機能追加を考慮した設計改善案
5. 自己レビュー(要件と成果物に矛盾がないか)

### ユーザーの開発環境
- **Windows PC**(Linuxではない。Claude側の検証はサンドボックス上のLinuxコンテナで行っているが、これはユーザー環境とは無関係)
- マーケットスピードII(楽天証券)がインストール済み・稼働中
- .NET SDK 9.0.316がインストール済み(net8.0/net8.0-windowsのビルドに使用)

---

## 3. これまでの経緯(時系列)

| # | 内容 | 結果 |
|---|---|---|
| 1 | 「株式デイトレを自動判断・自動売買するデスクトップアプリを作れるか」という初回相談 | 実現可能と回答。まずバックテストツールから着手する方針で合意 |
| 2 | **Python + Tkinter版**のバックテストツールを試作(CSV読込、SMAクロス/RSI戦略、matplotlib描画) | 動作確認済み。現在は開発の起点として過去の成果物扱い(現行はC#版に一本化) |
| 3 | 「Windows+Excel環境が前提なのでC#にリプレースしてほしい」との要望 | C#(当初WinForms想定)での再構築を開始 |
| 4 | ユーザーから「WinFormsでなくWPFにしてほしい」「なぜLinuxという言葉が出るのか」との質問、および**上記「役割・出力形式」の明示的な指定** | WPFへ方針転換。Linux云々はClaude側の検証用サンドボックスの話であり、ユーザー環境とは無関係と説明。以降、指定の役割・出力形式で回答を継続 |
| 5 | Core(判断ロジック)をC#に移植し、Python版と**数値が完全一致**することを確認 | Core実装完了・検証済み |
| 6 | WPF(MVVM)でのGUI実装、バックテストエンジン・指標計算のCore実装 | 実装完了(ただしこの時点ではこのサンドボックスでWPFの実機ビルド検証は不可能という制約付き) |
| 7 | データソースについて相談: CSV運用 vs J-Quants API(JPX公式)のメリット・デメリット比較 | J-Quants APIは無料プランだとデータが直近12週間分に制限される等の制約を確認。**当面CSV運用継続、将来的な拡張性のためIPriceDataSource抽象を導入する方針**で合意 |
| 8 | 「本番運用の自動発注は楽天証券RSSを使う予定。バックテストもRSSのデモ的な仕組みで代用できないか」との相談(選択肢①)。代替案として、MarketSpeed2の画面から歩み値・板情報をキャプチャする自作アプリ案(選択肢②)も提示された | 調査の結果、楽天の「デモ取引」は静的なサンプル画面に過ぎないが、**RSSの`RssChartPast`関数で過去のティック/分足データを取得できる**ことが判明。①(RSS連携)を採用する方針で合意。②は温存 |
| 9 | 技術方式の確認: COM連携はMicrosoft.Office.Interop.Excelを希望。「LibreOfficeでも良いか」との質問 | **LibreOffieは非対応(純正Excel必須)** と回答(RSSがExcel専用のxllアドインのため)。対象足種はティック/1分足/5分足、遡り期間は未定(実測で決定する方針)で確定 |
| 10 | `DayTradeBacktester.RakutenRss`プロジェクトの実装(Excel COM連携基盤) | 実装完了。ただしこの環境では実機ビルド検証が一切できない制約付き |
| 11 | 「より良い設計案を対応してほしい」との依頼 → STAスレッド対応・設定の外部化(JSON化)を実施 | `StaThreadDispatcher`(COM操作を専用STAスレッドに集約)、`RakutenRssOptions`(JSON設定ファイル)を実装 |
| 12 | 「NuGet.Configが含まれているが必要か」との指摘 | **私のミスと判明**。検証用サンドボックスのための回避策ファイルで、ユーザー環境ではNuGet復元を破壊する有害なファイルだった。削除して納品 |
| 13 | ユーザー環境での実ビルドを開始 → 複数のコンパイルエラーが発生 | 以下の3件を実際のビルドエラーから特定・修正(詳細は後述の「発生した問題と対応」参照) |
| 14 | **最終的にユーザー環境でビルド成功** | ✅ 2026-08-08時点の最新状態 |
| 15 | 今後の進め方(フェーズ0〜6のロードマップ)を提示 | ユーザーからの応答待ちの状態で、本ドキュメント作成の依頼を受け一時停止 |

---

## 4. 発生した問題と対応(トラブルシューティング履歴)

実機ビルドで発生した問題は、今後同種の実装をする際の教訓として記録する。

| # | エラー内容 | 原因 | 対応 |
|---|---|---|---|
| 1 | `NETSDK1004`(assets.jsonが見つからない)、`CS0006`(Core.dllが見つからない) | 単純に`dotnet restore`が未実施だった | `dotnet restore`の実行を案内(コードの問題ではなかった) |
| 2 | `CS0246 'OhlcvBar' が見つかりません` | `RssChartPastReader.cs`で`using DayTradeBacktester.Core.Models;`の書き忘れ | using文を追加(実装ミス) |
| 3 | `CS1061 'Range' に 'Formula2' の定義がありません` | `Microsoft.Office.Interop.Excel`のバージョン`15.0.4795.1001`(Office 2013相当)には`Formula2`が存在しない(Excel 2016以降で追加されたAPI) | パッケージバージョンを`16.0.18925.20022`に変更(パッケージ選定ミス) |
| 4 | `CS0117 'Marshal' に 'GetActiveObject' の定義がありません` | `Marshal.GetActiveObject`は.NET Framework専用APIで、.NET Core/.NET 5以降(net8.0-windows含む)には存在しない | `RunningObjectTable.cs`を新規作成し、Windows API(`CLSIDFromProgID`+`GetActiveObject`)へのP/Invokeで自前実装 |
| 5 | 納品ZIPに`NuGet.Config`(検証用の回避策、パッケージソースを全無効化する内容)が混入 | 検証用ファイルと納品物の分離ができていなかった | 削除。以降はZIP化前に「混入チェック」を実施する運用に変更 |

**教訓**: このサンドボックス環境(Linux、NuGet.orgへの接続制限あり)はWPF/Excel COMの実機ビルド検証ができないため、**最終的な正確性はユーザー環境でのビルド結果でしか確認できない**。今後も「実装→ユーザー側でビルド→エラー報告→修正」のサイクルを前提に進める。

---

## 5. 現在のアーキテクチャ

```
DayTradeBacktester/
├── DayTradeBacktester.Core/          … 判断ロジック・バックテストエンジン(UI非依存、Windows/Linux/Mac共通)
│   ├── Models/                       … OhlcvBar, SignalBar, TradeRecord, BacktestResult
│   ├── Strategies/                   … IStrategy, SmaCrossStrategy, RsiStrategy, StrategyRegistry
│   ├── Engine/                       … BacktestEngine(売買シミュレーション)
│   ├── Metrics/                      … MetricsCalculator(総リターン・ドローダウン・シャープレシオ等)
│   └── Data/                         … IPriceDataSource(抽象)、CsvDataLoader、CsvPriceDataSource、
│                                        RssColumnNames、RssRangeParser(RSS出力→OhlcvBar変換)
│
├── DayTradeBacktester.App/           … WPFデスクトップGUI(MVVM、Windows専用)
│   ├── Common/                       … ObservableObject, RelayCommand, StringToVisibilityConverter
│   ├── ViewModels/                   … MainViewModel, StrategyParamInput, SummaryItem
│   └── Views/                        … MainWindow.xaml(+.cs)
│   ※ チャート描画にOxyPlot.Wpfを使用
│
├── DayTradeBacktester.RakutenRss/    … マーケットスピードII RSS連携(Excel COM自動化、Windows専用)
│   ├── ExcelRssSession.cs            … Excel COM接続のライフサイクル管理
│   ├── RunningObjectTable.cs         … Marshal.GetActiveObject代替のP/Invoke実装
│   ├── RssFormulaBuilder.cs          … RssChartPast数式の組み立て
│   ├── RssChartPastReader.cs         … 数式書き込み・反映待ち・結果読み取り(STAスレッド前提の同期処理)
│   ├── StaThreadDispatcher.cs        … Excel COM操作を専用STAスレッドに集約する仕組み
│   ├── RakutenRssOptions.cs          … タイムアウト・列名等の外部設定(JSON)
│   ├── RakutenRssDataSource.cs       … IPriceDataSource実装(上記を束ねる窓口)
│   └── rakuten-rss-settings.sample.json
│
├── DayTradeBacktester.SmokeTest/     … Core動作確認用コンソールアプリ(開発用、Windows/Linux/Mac共通)
│   └── Program.cs                    … Python版との数値一致確認 + RssRangeParserのダミーデータ検証
│
├── sample_data/sample_stock.csv      … 動作確認用の合成株価データ
└── README.md                          … セットアップ手順・アーキテクチャ説明・既知の未検証事項
```

### 設計上の主要な意思決定

1. **データソースの抽象化**: `IPriceDataSource`インターフェースにより、CSV・RSS・将来のJ-Quants API等を同一の方法でCoreに供給できる設計。
2. **STAスレッドへの集約**: Excel COMはシングルスレッドアパートメント前提のため、`StaThreadDispatcher`が専用スレッドを1本保持し、接続〜COM解放までを同期的にそのスレッド上で実行する。呼び出し側(WPFのUIスレッド等)はスレッドの種類を意識せず`await`できる。
3. **設定の外部化**: タイムアウト値・列名等は`RakutenRssOptions`としてJSONファイルから読み込み可能(コード変更不要で実機調整できる)。
4. **列名の動的解決**: `RssRangeParser`はRSS出力のヘッダー行を読んで列位置を解決するため、実際の列並びが想定と異なっていても設定変更のみで対応できる。

---

## 6. 現在の状態(何が終わっていて、何が終わっていないか)

### ✅ 完了・検証済み
- Core(判断ロジック)は、Python版と完全に同一の数値が出ることを確認済み
- App(WPF)・RakutenRssを含む**全プロジェクトがユーザー環境で正常にビルド成功**(2026-08-08時点)
- `RssRangeParser`(RSS出力のパース処理)は、ダミーデータによる単体検証で正しく動作することを確認済み

### ⚠️ 未着手・未検証(重要)
- **`RssChartPast`関数の実機での挙動が一切未確認**。以下は全て未検証:
  - 実際のヘッダー列名・列順が想定(日付・時刻・始値・高値・安値・終値・出来高)と一致するか
  - ティック/1分足/5分足それぞれで実際に遡れる期間
  - 数式入力から反映までの実際の所要時間(現在のタイムアウト設定20秒が妥当か)
  - ティックデータ使用時の1日あたり行数と、確保しているバッファ(列10・行+5)で足りるか
- `RakutenRssDataSource`をWPFアプリに統合する作業(現状はCore/RakutenRssプロジェクトが独立して存在するのみで、GUIの「データ読込」ボタンからは呼べない)
- 実データでのバックテスト実施・戦略の有意性評価そのもの(まだ一度も実施していない)
- 本番発注機能(RSS発注関数の実装)は完全に未着手・スコープ外

---

## 7. 今後の作業フロー(ロードマップ)

| フェーズ | 内容 | 状態 |
|---|---|---|
| 0 | Core/App/RakutenRssのビルド成功 | ✅ 完了 |
| **1** | **Excel上で`RssChartPast`を手動実行し、実際の仕様(ヘッダー名・列順・遡れる期間・反映速度)を確認する** | 🔲 未着手(**再開時に最初に行うべき作業**) |
| 2 | `RakutenRssDataSource`単体での疎通確認用コンソールアプリ(`DayTradeBacktester.RakutenRss.SmokeTest`)の作成・実行 | 🔲 未着手 |
| 3 | フェーズ1/2の実測結果に基づき、`rakuten-rss-settings.json`(タイムアウト・列名等)を調整 | 🔲 フェーズ1/2待ち |
| 4 | WPF Appの「データ読込」セクションに「RSSから取得」ボタンを追加し、`RakutenRssDataSource`を統合 | 🔲 未着手 |
| 5 | 実データ(RSS経由の過去データ)でSMAクロス/RSI戦略のバックテストを実施し、有意性を評価 | 🔲 未着手 |
| 6(将来・別スコープ) | RSS発注関数を使った本番自動発注機能の実装(誤発注防止ガード必須) | 🔲 未着手 |

### 再開時の推奨アクション

1. まずフェーズ1(Excel手動確認)を実施し、結果(ヘッダー名・列順・遡れる期間・反映速度)を共有する
2. その結果を踏まえて、フェーズ2(疎通確認用コンソールアプリ)の実装に進む、または直接フェーズ3(設定調整)に進む

---

## 8. 保留中・未回答の論点

- **選択肢②(MarketSpeed2画面からの歩み値・板情報キャプチャ、WPFでのリアルタイムOCR的な実装)**: ①(RSS連携)が機能することが前提で温存中。①で技術的な壁(遡れる期間が短すぎる等)にぶつかった場合に検討する。
- ユーザーが過去に作成した「流用できそうなソースコード」: 本当に必要になったタイミングでのみ共有してもらう方針(現時点では未共有・未参照)。
- データソースとしてのJ-Quants API(JPX公式)は、CSV運用継続の方針により現時点では見送り。`IPriceDataSource`抽象があるため、将来必要になれば`JQuantsPriceDataSource`を追加するだけで対応可能。

---

## 9. 成果物(ファイル)

| ファイル | 内容 | 状態 |
|---|---|---|
| `daytrade_backtester.zip` | Python + Tkinter版バックテストツール(初期プロトタイプ) | 参考用。現行はC#版に一本化 |
| `DayTradeBacktester_CSharp_WPF.zip` | **現行のC#/WPFソリューション一式**(Core/App/RakutenRss/SmokeTest) | ✅ 最新版・ユーザー環境でビルド成功確認済み |

※ 再開時は`DayTradeBacktester_CSharp_WPF.zip`(最新版)をベースに進めてください。

---

## 10. 既知の環境制約(Claude側)

- Claudeの検証環境はLinux(Ubuntu)サンドボックスのため、WPF(`net8.0-windows`)・RakutenRss(Excel COM)プロジェクトの実機ビルドはこの環境では一切できない。Coreプロジェクトのみ検証可能。
- このサンドボックスはNuGet.orgへの接続が制限されているため、Core検証時も一時的な`NuGet.Config`が必要になることがある(納品物には含めないよう毎回確認する運用)。
- そのため、**RakutenRss/App関連の最終的な正しさは、常にユーザー環境でのビルド・実行結果でのみ確認できる**。今後もこの前提で進める。

---

## 11. 免責事項

本プロジェクトの成果物・分析結果は投資助言ではない。実際の投資判断は自己責任で行う。過去の成績は将来の成果を保証するものではない。
